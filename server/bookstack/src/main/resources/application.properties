# Bookstack Main Backend Configuration
# =========================================================================

# Application Identity
spring.application.name=bookstack

# Database Connectivity (MySQL)
# -------------------------------------------------------------------------
# Primary application database for managing book transactions, users, etc.
spring.datasource.url=jdbc:mysql://localhost:3306/bookstack?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=bookstack
spring.datasource.password=bookstack
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate Settings
# -------------------------------------------------------------------------
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.open-in-view=false
spring.jpa.show-sql=true
# Managed externally or via SQL scripts
spring.jpa.hibernate.ddl-auto=none

# Razorpay Integration (Payment Gateway)
# -------------------------------------------------------------------------
razorpay.key.id=rzp_test_SA8oZTwOs9GBZe
razorpay.key.secret=vUCjIKUDCZA4FHtyCajrOGxS
razorpay.currency=INR
razorpay.company=BookStack

# Email Server Settings (SMTP - Gmail)
# -------------------------------------------------------------------------
bookstack.from.email=dummybookstack@gmail.com
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=dummybookstack@gmail.com
spring.mail.password=oyus iurp zqqk jgax

# SMTP Protocol Settings
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# Kafka Integration (Producer)
# -------------------------------------------------------------------------
# Used to send notifications to the messaging service
spring.kafka.bootstrap-servers=localhost:9092
spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.producer.value-serializer=org.springframework.kafka.support.serializer.JsonSerializer
# Type mapping for JSON serialization of DTOs
spring.kafka.producer.properties.spring.json.type.mapping=email:com.project.bookstack.dto.EmailDTO