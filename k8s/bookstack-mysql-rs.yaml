apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: mysql-bookstack-rs
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mysql-bookstack
  template:
    metadata:
      labels:
        app: mysql-bookstack
    spec:
      containers:
        - name: mysql
          image: mysql:8.0
          env:
            - name: MYSQL_ROOT_PASSWORD
              value: bookstack
            - name: MYSQL_DATABASE
              value: bookstack
          ports:
            - containerPort: 3306
